# vpn-speed-test-automation
An open-source Windows automation tool to benchmark and compare VPN protocols using the official Ookla Speedtest CLI. It automatically runs sequential tests, parses JSON metrics via PowerShell, and logs results into a CSV.
